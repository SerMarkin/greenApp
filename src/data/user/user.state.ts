export interface UserState {
  isLoggedin: boolean;
  username?: string;
  darkMode: boolean;
  swipeMode: boolean;
  hasSeenTutorial: boolean;
  loading: boolean;
  addUserdata:string;
  scorePaper:number;
};
